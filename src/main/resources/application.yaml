spring:
  datasource:
    username: postgres
    password: 5784
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/course_db
  jpa:
    hibernate:
      ddl-auto: none
server:
  port: 8080